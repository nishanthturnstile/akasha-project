# Akasha STAC API — stac-fastapi-pgstac environment placeholders.
# These are the real variable names consumed by stac-fastapi-pgstac.

POSTGRES_HOST_READER=postgis.railway.internal
POSTGRES_HOST_WRITER=postgis.railway.internal
POSTGRES_PORT=5432
POSTGRES_USER=<user>
POSTGRES_PASS=<password>
POSTGRES_DBNAME=<db>

APP_HOST=0.0.0.0
APP_PORT=8080