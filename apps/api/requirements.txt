# Akasha BFF (api) — dependencies.
# Pinned for reproducible Railway builds.
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-dotenv==1.0.1
# Slice 1: app-schema migration CLI only (no product DB endpoints yet).
# Imported lazily so `app.main` (runtime/preview) never requires a DB driver.
psycopg[binary]>=3.2,<4
# Slice 2 (Phase 2 raster de-risk): BFF masked-statistics + RGB tile proxy.
# rasterio reads the analytic + SCL COG windows; numpy does the masked index
# math; shapely/pyproj validate geometry + compute area. rio-tiler is available
# for raster helpers. All imported LAZILY in app.raster.* so importing app.main
# (the live preview) never requires these geospatial wheels.
numpy==2.4.6
rasterio==1.4.3
rio-tiler==9.0.6
shapely==2.1.2
pyproj==3.7.2
