# Akasha ingestion worker — Slice 1 dependencies (pinned for Railway).
# pypgstac MUST match the running stac-fastapi-pgstac:5.0.2 schema (>=0.8,<0.10).
python-dotenv==1.0.1
pypgstac[psycopg]>=0.9,<0.10
boto3>=1.35,<2
# Heavy raster deps (rasterio, rio-cogeo) are added in Slice 2 with COG creation.
