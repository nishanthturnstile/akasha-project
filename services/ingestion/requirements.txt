# Akasha ingestion worker — Slice 1 dependencies (pinned for reproducible builds).
# pypgstac MUST match the running stac-fastapi-pgstac:5.0.2 schema (>=0.8,<0.10).
python-dotenv==1.0.1
pypgstac[psycopg]>=0.9,<0.10
boto3>=1.35,<2
# Slice 2 raster preparation: SAFE/JP2 -> validated COGs.
# Keep these pinned together so GDAL/rasterio/rio-cogeo remain compatible.
numpy>=1.26,<3
rasterio==1.4.3
rio-cogeo==5.4.2
